@model Workhous.Models.ProjectTimeEntry

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TimeEntry</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model=>model.ProjectName)
        </dt>
         <dd>
             @Html.DisplayFor(model => model.ProjectName)
         </dd>
        <dt>
            @Html.DisplayNameFor(model => model.TimeEntry.Day)
        </dt>           
        <dd>
            @Model.TimeEntry.Day.ToString("dd.MM.yyyy")
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TimeEntry.NumOfHours)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimeEntry.NumOfHours)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
